Cd("::/Home/Erythros");;

Option(OPTf_WARN_PAREN, OFF);
Option(OPTf_WARN_DUP_TYPES, OFF);

try
{
	#include "Run"
}
catch
{
	Fs->catch_except = TRUE;
	"\nErythros boot failed; ZealOS terminal remains available.\n";
}

Option(OPTf_WARN_PAREN, ON);
Option(OPTf_WARN_DUP_TYPES, ON);
